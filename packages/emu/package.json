{
  "name": "@protopulse/emu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCU emulation (Vol II §D.2): cycle-accurate AVR cores via avr8js behind the McuCore contract. Firmware runs for real; pins and UART are observable.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "avr8js": "^0.21.0",
    "zod": "^3.25.76"
  }
}
