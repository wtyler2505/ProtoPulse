---
summary: HC-SR501 PIR motion sensor — web-verified 3-pin layout for the @protopulse/parts seed library
category: hardware
provenance: verified
source: components101 + utmel + handsontec HC-SR501 datasheet (web-verified)
verified: 2026-06-22
---

# HC-SR501 PIR motion sensor — verification note

Added `core:hc-sr501` to `@protopulse/parts`. Classic 3-pin PIR motion module (Tyler owns
it — "HC-SR501 PIR Motion Sensor Module" in the component log). Pinout **web-verified**.

## Part
- **MPN:** HC-SR501 (BISS0001-based pyroelectric PIR)
- **Class:** `ic`, refPrefix **U** (module). 3 pins. Footprint deferred.

## Pinout — 3-pin header
`VCC, OUT, GND`. The **middle pin is always OUT** (the reliable invariant); some clones
silk-swap the VCC/GND ends, so modeled the canonical components101 VCC/OUT/GND order and
flagged the caveat in the provenanceNote.

## Behavior / ERC modeling
- **VCC, GND → power_in.** 4.5–20 V supply (onboard regulator; 5 V typical from Arduino).
- **OUT → output.** Digital HIGH (~3.3 V TTL) when motion detected, LOW idle; module-driven.
- On-board: 2 trim pots (sensitivity + time delay) and an H/L retrigger jumper — not header
  pins, not modeled. ~7 m range, ~120° FoV, 30–60 s warm-up.

## Verification
- Web sources: components101 HC-SR501, utmel, handsontec datasheet — agree VCC/OUT/GND.
- Component log agrees; web is the authority per the standing rule.
- Vault (`qmd_search`): no prior HC-SR501 note.

## Pipeline
Next: route to `knowledge/` via `/extract` as `hardware-component-hc-sr501.md`
(never author `knowledge/` directly).
