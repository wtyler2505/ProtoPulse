node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
__pycache__/
*.pyc

# Product analysis working directory (generated phase outputs)
.claude/analysis/

# Playwright
e2e-results/
e2e/.auth-state.json
# qmd semantic search index (regenerated via: qmd update protopulse-vault)
.qmd.db
src-tauri/target/
src-tauri/Cargo.lock
.superpowers/

# DB backups (pre-migration dumps)
backups/

# Test run logs (persistent output for multi-agent context sharing)
logs/
.moltbook_credentials.json

# Local MCP runtime caches
FileScopeMCP-tree.json

# R4 retro Wave 8 (C12+C23): downloaded Arduino CLI sidecar binaries are
# fetched per-target at CI build time. Not committed to repo.
src-tauri/binaries/arduino-cli-*
