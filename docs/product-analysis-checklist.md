# ProtoPulse Product Analysis Checklist

> Baseline refreshed: 2026-07-18
>
> The earlier checklist reached 166/166 completed items on 2026-03-07 and remains available in git history. This is a new baseline; none of those historical rows are reopened or blended into this set.
>
> Every row below is required. P0 through P3 describe dependency/build stages only. They do not rank value or grant permission to omit later work.
>
> A P3 experiment is complete when it produces evidence and a documented `ship`, `reshape`, or `shelf` result.
>
> Effort scale, smallest to largest: `S|M|L|XL` (hours, days, weeks, months).
>
> Cross-category repetition is intentional lens evidence. The 12 implementation bundles in the report coordinate shared work without deleting any row.

| Category | Count |
|---|---:|
| Outside-inspiration findings (FG) | 20 |
| UX issues (UI) | 17 |
| Technical debt (TD) | 28 |
| Enhancements (EN) | 23 |
| Innovations (IN) | 20 |
| **Total** | **108** |

## Outside-Inspiration Findings (FG)

- [ ] FG-01: Implement the `.ppx` compatibility group as one reviewed unit: content-addressed assets, explicit version dispatch and forward migrations, unknown-operation preserve/quarantine policy, and frozen cross-version fixtures for directory, browser-bundle, and packaged-Tauri paths. | Effort: XL | Priority: P0
- [ ] FG-02: Make saved-design failure non-destructive in browser and packaged Tauri: retain the unreadable payload, suspend autosave, show recovery/export/migration choices, require an explicit reset before a starter may replace local data, and reuse this preserve-first behavior for every imported capsule/pack. | Effort: M | Priority: P0
- [ ] FG-03: Deliver a fail-closed, proof-carrying agent edit path: add ordinary change/remove/disconnect tools, require approval for every mutating proposal, apply into a proposal branch, attach sources and rationale, and record ERC/DRC/review/simulation results before merge. | Effort: XL | Priority: P0
- [ ] FG-04: Add a thin private provider gateway with key custody, scoped access, audit events, and a documented self-host path while keeping `.ppx` files and local editing independent of that service. | Effort: L | Priority: P0
- [ ] FG-05: Add typed intent, requirement, tolerance, constraint, and verification-evidence entities to the graph; trace each requirement to checks, scenarios, components, nets, and review decisions. | Effort: XL | Priority: P1
- [ ] FG-06: Build a versioned portable review capsule containing a frozen branch reference, purpose, design/fabrication diffs, deck results, checklists, stable-anchor comments, executable fixes, decisions, and closure; preserve invalid/future imports, block destructive merge, and apply relay roles/quotas when transported through a room. | Effort: XL | Priority: P1
- [ ] FG-07: Add a data-only deterministic `protopulse run` scenario format and CLI command with scripted inputs, serial/bus/GPIO/analog assertions, timeouts, fidelity metadata, stable artifacts/exit codes, and production-policy browser plus packaged-Tauri tests for any extension surface. | Effort: XL | Priority: P1
- [ ] FG-08: Build a causal trace that aligns graph-operation history, firmware program counter/register state, bus transactions, GPIO edges, analog nodes, and scenario assertions on one navigable timeline. | Effort: XL | Priority: P2
- [ ] FG-09: Create a sandboxed device-model workshop for I2C, SPI, GPIO, and analog behavior using a constrained interpreter/AST evaluator or isolated process—not browser string execution—with register-map helpers, traces, deterministic fixtures, resource limits, fidelity declarations, pack integration, and production-CSP browser/Tauri proof. | Effort: XL | Priority: P1
- [ ] FG-10: Expand part/device packs into a cross-validated multi-projection schema covering electrical pins, symbol, footprint, breadboard shape, 3D/SPICE/firmware models, parametrics, tutorial, and license; every field references canonical Ars Contexta evidence with revision/hash and verification state. | Effort: XL | Priority: P1
- [ ] FG-11: Define a versioned reusable design-capsule format containing graph operations, schematic/PCB fragments, requirements, data-only scenarios, review, BOM alternatives, canonical evidence references, license, and curriculum; make fork/import produce typed operations and route unsupported/corrupt input through non-destructive recovery. | Effort: XL | Priority: P2
- [ ] FG-12: Add the engine architecture projection for system blocks, interfaces, budgets, requirements, and graph drill-down, then prove it against representative legacy architecture projects. | Effort: XL | Priority: P1
- [ ] FG-13: Add the engine breadboard/build projection with exact physical dimensions and pin geometry, guided placement/wiring, continuity observations, photo overlays, and discrepancy capture as reviewable operations/evidence. | Effort: XL | Priority: P1
- [ ] FG-14: Implement version-pinned editable KiCad schematic/board import and export, golden round-trip fixtures, explicit unsupported-feature reports, and an embeddable read-only projection. | Effort: XL | Priority: P2
- [ ] FG-15: Make accessibility an intentional projection of the canonical graph plus transient tool/selection state: land reflow, focus-visible controls, real tab semantics, and a named canvas region first, then semantic DOM entities, keyboard-equivalent edits, and announced operations; add no parallel stored accessibility model and validate in browser plus packaged Tauri. | Effort: L | Priority: P1
- [ ] FG-16: Harden local-first collaboration with explicit bind policy, mandatory non-loopback auth, room-scoped capabilities and read/comment/edit roles, client/room/branch/envelope/byte and storage quotas, backpressure/eviction, durable offline queues, preserve-first conflict branches, resource tests, and file exchange without a relay. | Effort: XL | Priority: P1
- [ ] FG-17: Bind machine-checkable evidence to the existing knowledge path: route raw material through `inbox/` → `/extract` → `knowledge/`, use the PP-NLM bidirectional bridge for versioned synthesis, and let parts/decks/checks/releases store only resolvable source IDs, dates, revisions/hashes, applicability, recheck/supersession state, and license compatibility. | Effort: L | Priority: P2
- [ ] FG-18: Run Tyler's representative legacy projects through the importer and new editor, preserve a lossless before/after evidence packet, log every parity and data issue, and make successful architecture/breadboard/persistence coverage gate the canonical-UI transition. | Effort: XL | Priority: P2
- [ ] FG-19: Execute the ADR-0017 hypothesis ladder after FG-01: validate real firmware co-design workflows first, then run bounded enclosure/CadQuery-or-JSCAD and URDF link/joint spikes only if the same-user evidence supports them; record a ship, reshape, or shelf decision at each gate and preserve positive and negative results through the existing Ars Contexta and PP-NLM path. | Effort: L | Priority: P3
- [ ] FG-20: Produce a reproducible open-hardware release capsule with exact `.ppx`, pack/deck/model revisions, canonical evidence IDs, source/license manifest, review/check/scenario evidence, fabrication outputs, hashes/signature, policy-equivalent clean-environment proof, and honest OSHWA/ordering status that never implies an external action completed when it did not. | Effort: L | Priority: P2

## UX Issues (UI)

- [ ] UI-01: Restore Vault, Labs, Audit Trail, Supply Chain, BOM Templates, My Parts, Alternates, and Part Usage in legacy now, add a visible/preview/retired invariant, and map every view to an engine capability projection or explicit retirement; derive engine navigation from that single registry. | Effort: M | Priority: P0
- [ ] UI-02: Implement “Open sample” now in legacy so it transactionally creates the declared nodes, edges, and BOM, shows progress/failure, and lands on a populated step; freeze every sample as a fixture that the engine must recreate through typed operations before flip. | Effort: L | Priority: P0
- [ ] UI-03: Remove the rich legacy template claim now and label the actual description-only behavior; recreate DRC, BOM, export, naming, and component policies as typed engine operations with a saved summary before flip. | Effort: L | Priority: P0
- [ ] UI-04: Remove/disable every handlerless legacy radial action now; recreate the useful commands in the engine with eligibility, typed operations, undo, success/failure state, and command-discoverability evidence. | Effort: L | Priority: P0
- [ ] UI-05: Hide legacy milestone facts backed only by hard-coded flags now; recreate progress as a graph-derived engine projection over operations, checks, simulation, export, PCB, fab, variables, review, and community evidence, with partial-project re-entry tests. | Effort: L | Priority: P0
- [ ] UI-06: Define and render one explicit product transition with a per-capability disposition ledger: identify the current canonical editor, provide loss-reporting import/handoff, prove real projects and required projections, then execute the default flip and read-only grace period without dropping any legacy promise silently. | Effort: XL | Priority: P0
- [ ] UI-07: Make the engine editor reflow safely: collapsible/resizable palette and inspector, persisted widths, usable narrow layouts, and no clipped/trapped controls. | Effort: L | Priority: P1
- [ ] UI-08: Replace the 17–18 equal-width strip with a scalable capability/projection registry and hierarchy/overflow model; derive panels from it and implement WAI-ARIA tabs, arrow navigation, active announcement, and panel relationships. | Effort: L | Priority: P1
- [ ] UI-09: Build semantic circuit mode as a projection of the canonical graph plus ephemeral selection/tool/status state—not a parallel stored model: focusable named canvas, graph-derived DOM tree, announcements, keyboard commands, nonvisual properties, equivalent edits, and automated/manual assistive-technology evidence. | Effort: XL | Priority: P1
- [ ] UI-10: Add explicit `:focus-visible` treatment for engine buttons, inputs, tabs, canvas, and tool controls, then verify it with a keyboard-only walkthrough and contrast checks. | Effort: M | Priority: P1
- [ ] UI-11: Hide/disable Draftsman now, then connect it to the proof-carrying crew runtime with real provider/tool/approval state; remove the window-global hook and expose the tab only when a user can complete and review the workflow. | Effort: M | Priority: P1
- [ ] UI-12: Remove Settings from normal legacy navigation or label it Preview now; recreate canonical profile/appearance controls with validation/persistence and put provider secrets behind the private gateway before exposing API-key settings. | Effort: L | Priority: P1
- [ ] UI-13: Redirect malformed or non-positive project IDs to the project picker, preserve the attempted context where safe, and explain the correction instead of guessing project 1. | Effort: S | Priority: P1
- [ ] UI-14: Remove the hidden `/api/seed` wait from project entry so loading reflects the requested project only; move development fixtures to an explicit bootstrap flow with visible failure. | Effort: M | Priority: P1
- [ ] UI-15: Replace the legacy project-card `div role=button` pattern with semantic sibling controls: a real project link/button plus a separately focusable, always focus-revealable archive/restore action. | Effort: M | Priority: P2
- [ ] UI-16: Add machine-readable capability states (`available`, `local-only`, `emulated`, `preview`, `blocked`, `live`) and honest empty states now for flashing, community, board packs, quotes, and ordering; derive navigation/claims from integration truth and enable `live` only when each engine path returns verifiable evidence. | Effort: M | Priority: P2
- [ ] UI-17: Add an engine recovery screen for unreadable/future-version saved designs that preserves the original payload and offers export, retry/migrate, or explicit reset before any starter is flushed. | Effort: L | Priority: P2

## Technical Debt (TD)

- [ ] TD-01: Close the Arduino process boundary: validate FQBN, port, sketch path, artifact path, and filename with one request schema; resolve every path beneath the owned project workspace; replace the syntax-check and `nm` shell strings with `execFile` argument arrays; add quote, traversal, option-injection, symlink, and cross-project tests; gate honest real-device flashing/capability labels on this result. | Effort: M | Priority: P0
- [ ] TD-02: Remove both blocking `quick-xml@0.38.4` advisories through an upstream Tauri/plist-compatible upgrade or reviewed dependency patch; verify every Tauri target still packages and `cargo audit` is clean for those IDs, while preserving the distinction that packaging succeeds today but release evidence does not. | Effort: M | Priority: P0
- [ ] TD-03: Address all 23 current Rust audit warnings: upgrade `anyhow`, `glib`, both `rand` branches, replace or upgrade the 19 unmaintained crates, or record a precise time-boxed exception with reachability and recheck evidence for each one. | Effort: L | Priority: P0
- [ ] TD-04: Make the supply-chain runner collect Rust audit, npm audit-policy, and both SBOM results before returning one aggregate status; align the documented severity policy with the actual `cargo audit` behavior so a Rust failure no longer hides npm/SBOM state, and feed that complete record into the release-readiness artifact. | Effort: M | Priority: P0
- [ ] TD-05: Resolve every current npm affected family, not only the five high nodes: upgrade the fixable `js-yaml` and DOMPurify chains; re-evaluate/remove/renew each Genkit/OpenTelemetry/Google/uuid exception before 2026-07-31; keep exact path, reachability, no-fix proof, owner, and expiry for any remaining exception. | Effort: L | Priority: P0
- [ ] TD-06: Replace DRC and circuit-DSL string execution with a constrained interpreter/AST evaluator or isolated process boundary; retain time/output/resource caps and adversarial sandbox tests; prove both features in a production-CSP browser and packaged Tauri without adding `'unsafe-eval'`; reuse the resulting trust policy and resource limits in future device-model work instead of adding another raw string-execution path. | Effort: XL | Priority: P0
- [ ] TD-07: Implement explicit `.ppx` version dispatch and ordered migrations, reject unsupported future versions into recovery rather than normal load, and freeze cross-version fixtures for every shipped version; treat TD-07 through TD-10 as one gate for the real-project migration corpus and any stored-vocabulary expansion. | Effort: L | Priority: P0
- [ ] TD-08: Define and implement an unknown-operation policy that preserves/quarantines unrecognized op records with diagnostics instead of losing them or hard-failing the whole log; cover nested `batch` cases and round trips. | Effort: L | Priority: P0
- [ ] TD-09: Add non-destructive editor recovery for malformed/future bundles: retain the original bytes, block autosave, offer export/retry/migrate/explicit reset, and test that a starter can never overwrite the only saved copy; consume the same recovery fixtures in migration and canonical-editor proving. | Effort: L | Priority: P0
- [ ] TD-10: Implement the documented content-addressed asset store and graph-reference integrity checks, including missing/orphan/hash-mismatch behavior and portable directory/packed-bundle fixtures; do not leave `assets/` as an empty directory contract. | Effort: XL | Priority: P0
- [ ] TD-11: Harden relay deployment: explicit bind host, mandatory auth outside loopback, room-scoped credentials/roles, client/room/branch/envelope/byte quotas, backpressure, idle eviction, storage quotas, and resource-exhaustion tests; preserve offline queues and account-free review-file exchange so the relay carries rather than owns collaboration. | Effort: L | Priority: P0
- [ ] TD-12: Upgrade the DOMPurify chain, rerun the malicious SVG corpus, and keep shared/client sanitizer configs in parity; replace Arduino autocomplete `innerHTML` with DOM nodes and `textContent`, then add adversarial component/net/name tests. | Effort: M | Priority: P0
- [ ] TD-13: Add a required Playwright CI lane for core legacy and engine journeys, keyboard/a11y checks, production CSP custom-code behavior, migration recovery, and critical import/export paths; publish trace/screenshots on failure and make the results part of canonical-editor/migration acceptance. | Effort: L | Priority: P1
- [ ] TD-14: Run database-backed migration parity in a named CI service lane and make its skipped/unavailable state explicit; likewise make the closed-loop cosim prerequisite/report explicit instead of silently conditional, because green unit volume cannot stand in for real-project migration or scenario/causal-trace evidence. | Effort: M | Priority: P1
- [ ] TD-15: Move packaged desktop smoke before fail-fast reporting or make it independently scheduled, remove `continue-on-error` after stabilizing it, and require one current packaged-smoke artifact per release target family so “packaged” and “release-ready” remain separate, provable states. | Effort: M | Priority: P1
- [ ] TD-16: Expose the ESLint warning inventory in CI, remove the duplicate `no-alert` override, establish a non-increasing warning baseline, and ratchet warnings to errors by changed file/package rather than hiding them with `--quiet`. | Effort: M | Priority: P1
- [ ] TD-17: Remove the current explicit-`any` and generated `as any` escapes at their source, then ratchet the 118 `!.`-shaped non-null assertions by requiring local guards or typed invariants in every changed file. | Effort: M | Priority: P1
- [ ] TD-18: Route all 22 browser/engine raw console calls through the existing redacting structured logger, preserving intentional terminal output in server/CLI/sample tools; add a source-only lint rule that distinguishes those scopes. | Effort: S | Priority: P1
- [ ] TD-19: Close all five actual developer TODOs with the promised data-model/UI behavior or convert each to a durable backlog ID and explicit unavailable state; preserve generated firmware-scaffold TODOs because those are intentional user instructions. | Effort: M | Priority: P1
- [ ] TD-20: Decompose the stateful UI hotspots into tested hooks/controllers/renderers: breadboard canvas, engine `CanvasHost`, PCB layout, Component Editor, Serial Monitor, Waveform Viewer, Chat, Architecture, Shape Canvas, and the client Export panel; preserve keyboard, undo, clipboard, selection, rendering, and migration-parity behavior with characterization tests before UX reflow or semantic-circuit work moves code. | Effort: XL | Priority: P1
- [ ] TD-21: Decompose the numerical/analysis hotspots behind stable typed interfaces and golden vectors: transient analysis, circuit solver, AC analysis, SPICE parser, shared DRC, Arduino pin formatting, breadboard audit, proactive healing, parametric search, PCB DRC, assembly cost, code simulator, WebGL viewer, alternate parts, tutorial, and LCSC mapping. | Effort: XL | Priority: P1
- [ ] TD-22: Partition `packages/emu/src/esp32s3.ts` and its 13,408-line test corpus by core/peripheral/crypto/instruction domain; retain deterministic fixtures and run package tests after each extraction; use the same partition as a code-ownership map because these files also have the highest measured change frequency and underpin scenario CI, device models, causal traces, and firmware hypothesis work. | Effort: XL | Priority: P1
- [ ] TD-23: Partition data-heavy/catalog boundaries—parts seeds, Arduino example circuits, and the shared schema—into discoverable generated/domain modules; document and test any large exporter intentionally retained as one serialization unit instead of splitting it mechanically. | Effort: L | Priority: P1
- [ ] TD-24: Add risk-specific coverage gates for auth/ownership, graph migrations/recovery, import/export, shell/process boundaries, relay quotas, and storage migration; keep the aggregate 60/50/55/60 floor but do not let it stand in for critical-path coverage. | Effort: L | Priority: P1
- [ ] TD-25: Replace the false-green package hooks with exit-code-aware, package-scoped typecheck/test commands that mark termination and “no tests found” as inconclusive/failure; keep the documented earlyoom-safe commands as the single implementation, not tribal knowledge. | Effort: M | Priority: P1
- [ ] TD-26: Execute one architecture migration plan for the legacy app and engine: prove representative real projects, define the canonical design/editor, route new AI work only through `@protopulse/ai`, freeze/retire duplicated legacy paths, and record parity/loss evidence before each default flip. | Effort: XL | Priority: P2
- [ ] TD-27: Establish local cache hygiene with bounded/age-based cleanup for the 26 GB Tauri target tree and stale build output, while preserving reproducible caches needed for active work; report source, dependency, target, and shipped bundle sizes separately. | Effort: S | Priority: P2
- [ ] TD-28: Document ownership and recovery notes for the full complexity inventory and the two high-change-frequency emulator files; archive or label the two June backup branches once their recovery value is verified, without deleting them blindly. | Effort: S | Priority: P2

## Enhancements (EN)

- [ ] EN-01: Add `.ppx` version dispatch and forward migrations, an explicit unknown-op quarantine/preserve policy, a content-addressed asset store with op references, and frozen cross-version fixtures before expanding the graph vocabulary. | Effort: XL | Priority: P0
- [ ] EN-02: Make engine startup recovery non-destructive: retain the unreadable payload, stop unconditional starter overwrite, present export/reset/retry choices, and test corrupt plus future-version bundles. | Effort: L | Priority: P0
- [ ] EN-03: Replace the implicit per-project `/api/seed` gate with an explicit, idempotent development bootstrap that does not delay every project load or mutate the first unrelated project. | Effort: M | Priority: P0
- [ ] EN-04: Implement sample initialization now in the shipping legacy app: transactionally persist every declared node, edge, and BOM item, report partial failure safely, freeze all five samples as canonical parity fixtures, and require engine import/typed-op creation to materialize the same expected designs before the default flip. | Effort: L | Priority: P1
- [ ] EN-05: Immediately reduce the legacy template UI to its real description-only behavior; define a canonical template contract, then recreate DRC, BOM, export, naming, and suggested-component application as typed engine operations/policies before the default flip—backport full legacy persistence only if templates must remain active through grace. | Effort: L | Priority: P1
- [ ] EN-06: Repair legacy reachability now by restoring the eight omitted working views and adding an explicit visible/preview/retired invariant; create one engine capability/projection registry that derives navigation and maps every legacy view to engine parity or deliberate retirement without duplicating design truth. | Effort: M | Priority: P1
- [ ] EN-07: Hide or label Draftsman unavailable immediately, then wire the panel to the shipped `@protopulse/ai` runtime through the proof-carrying provider/tool/approval boundary; remove the hidden global-hook dependency before exposing the tab. | Effort: M | Priority: P1
- [ ] EN-08: Complete the migration milestone with a per-capability disposition ledger: run Tyler's real projects through the importer, record loss/parity reports, require every legacy promise to be implemented in engine or explicitly retired, flip only after acceptance, keep read-only grace, and retire areas through ADRs. | Effort: XL | Priority: P1
- [ ] EN-09: Keep ADR-0017 outside current product claims until Tyler decides it; if accepted, assign backlog ownership and require the schema-evolution gate plus a cheap test of the same-user assumption before vertical work. | Effort: S | Priority: P1
- [ ] EN-10: Label firmware authoring/emulation separately from live flashing now, then finish and verify WebSerial/WebUSB on named supported hardware with permission, disconnect, retry, and failure-state tests before enabling the engine capability state. | Effort: L | Priority: P2
- [ ] EN-11: Close or explicitly bound the opportunistic ESP32 emulator long tail so support claims enumerate remaining GDMA, sleep/wake, interrupt, and peripheral limits. | Effort: XL | Priority: P2
- [ ] EN-12: Label current part-pack travel `local-only` now, then decide and implement the engine publication model—hosting, identity, provenance, moderation, removal, and versioning—before enabling a live Community state. | Effort: XL | Priority: P2
- [ ] EN-13: Build the verified board/module starter packs named in the roadmap after the ESP32 dependency is settled, preserving the hardware verification protocol for every physical claim. | Effort: XL | Priority: P2
- [ ] EN-14: Label the current path export-only/blocked now; decide the quote/order account and API model, then add freshness, vendor failure, consent, and returned-evidence tests before enabling a live engine ordering state. | Effort: XL | Priority: P2
- [ ] EN-15: Remove `/settings` from normal legacy navigation or label it Preview now; recreate only the canonical editor's real profile/appearance controls and route provider secrets through the private gateway boundary before exposing API-key settings. | Effort: L | Priority: P2
- [ ] EN-16: Resolve or formally exception the current Tauri `cargo audit` blockers and require a green desktop release-evidence lane before describing desktop delivery as healthy. | Effort: M | Priority: P2
- [ ] EN-17: Complete the remaining manual `pcbnew` import verification and record the exact fixture, output, and acceptance result in the golden-contract evidence trail. | Effort: S | Priority: P2
- [ ] EN-18: Replace hand-maintained repository counts and maturity prose with one generated current-state snapshot that labels generation time and separates shipped, in-progress, proposed, and historical truth. | Effort: M | Priority: P3
- [ ] EN-19: Add a source-only complexity report that runs bounded per area, publishes real `lizard` and `scc` artifacts, tracks every current hotspot/1,000-line boundary, and fails only on newly worsened budgets; never scan dependency/build trees or turn the metric into a mandate for arbitrary splitting. | Effort: M | Priority: P2
- [ ] EN-20: Add bundle analysis artifacts and route-level budgets for `WebGLBoardViewer`, `CodeEditor`, the main index, React vendor, and Breadboard; inspect lazy-loading/tree-shaking first and prove startup/interaction gains before changing chunk strategy. | Effort: M | Priority: P2
- [ ] EN-21: Publish one release-readiness artifact that shows platform package results, Rust and npm advisory state, exception expiries, SBOM hashes, packaged-smoke results, signature/attestation state, and links to exact logs; explicitly distinguish “all platform packages built” from “release evidence passed” so a green/red workflow is never the only explanation. | Effort: M | Priority: P2
- [ ] EN-22: Add a dependency-footprint report by capability family—Genkit/Google/OTel, renderer/3D, desktop/Tauri, simulation—so upgrades or removals can be judged by shipped reachability, install cost, advisories, and product use instead of package count alone. | Effort: M | Priority: P3
- [ ] EN-23: Add a security-regression corpus covering CSP, SVG/HTML injection, Arduino path/argument injection, relay exhaustion/auth, secret fixtures, and graph future-version recovery; run the relevant subset in every affected package and the complete corpus in CI. | Effort: L | Priority: P3

## Innovations (IN)

- [ ] IN-01: Ship evolution-safe `.ppx`: asset hashing, version dispatch/migrations, future-operation preserve/quarantine, cross-version directory/browser/Tauri fixtures, and one non-destructive recovery contract for saved data, imported capsules/packs, and sync conflicts that blocks writes until the user chooses an action. | Effort: XL | Priority: P0
- [ ] IN-02: Ship proof-carrying crew change sets: complete normal edit/remove tools, mandatory mutation approval, proposal branches, canonical Ars evidence IDs/rationale, exact diffs, production-policy verification runs, fidelity notes, and explicit merge. | Effort: XL | Priority: P0
- [ ] IN-03: Add a thin private provider gateway with safe key custody, scoped identity/budgets/audit, streaming, and self-host support while keeping local files and non-provider workflows service-independent; require separate packaged-smoke/audit evidence before claiming native desktop credential handling. | Effort: L | Priority: P0
- [ ] IN-04: Add typed intent, requirement, tolerance, assumption, verification-method, and evidence entities with trace links to graph entities, checks, scenarios, review decisions, and releases. | Effort: XL | Priority: P1
- [ ] IN-05: Build versioned portable review request/response capsules with frozen branch state, purpose, comparisons, checks, stable-anchor comments, decisions, and closure; preserve invalid/future inputs, block destructive merge, and apply IN-12 roles/quotas to room transport. | Effort: XL | Priority: P1
- [ ] IN-06: Add a data-only circuit-scenario schema and `protopulse run` with scripted inputs, serial/bus/GPIO/analog/requirement assertions, stable artifacts/exits, fidelity metadata, and production-CSP browser plus packaged-Tauri proof for any extension surface. | Effort: XL | Priority: P1
- [ ] IN-07: Build a resource-bounded device-model workshop for I2C/SPI/GPIO/analog behavior using a constrained interpreter/AST evaluator or isolated process—not string execution—with register helpers, deterministic state/time, traces, fixtures, fidelity declarations, pack loading, and production-CSP browser/Tauri smoke. | Effort: XL | Priority: P1
- [ ] IN-08: Expand part packs into living multi-projection part/device packages with electrical facts, symbol, footprint, breadboard/3D/SPICE/firmware models, parametrics, licenses, cross-projection tests, and field-level IDs resolving to canonical Ars Contexta evidence; execute models through IN-07. | Effort: XL | Priority: P1
- [ ] IN-09: Deliver architecture as a live graph projection with system blocks, interfaces, budgets, requirements, evidence state, and drill-down into schematic/PCB/firmware/review, then prove legacy-project parity. | Effort: XL | Priority: P1
- [ ] IN-10: Deliver dimension-verified breadboard build mode with legal placement/wiring, guided steps, continuity and measurement observations, photo attachments, and reversible intended-versus-observed discrepancy records. | Effort: XL | Priority: P1
- [ ] IN-11: Deliver semantic circuit projection: first land reflow, visible focus, real tabs, and a named canvas; then derive DOM entities and equivalent keyboard edits from canonical graph plus transient UI state, add no parallel stored model, keep selection synchronized, and validate in browser plus packaged Tauri. | Effort: L | Priority: P1
- [ ] IN-12: Harden local-first collaboration with explicit bind policy, mandatory non-loopback auth, room-scoped roles, actor identity, client/room/branch/envelope/byte/storage quotas, backpressure/eviction, durable queues, preserve-first conflict branches, resource tests, and account-free review file exchange. | Effort: XL | Priority: P1
- [ ] IN-13: Build the causal time machine aligning operation history, firmware source/program counter/registers, interrupts/buses, GPIO, analog waveforms, device state, and assertions; preserve and diff traces as scenario artifacts. | Effort: XL | Priority: P2
- [ ] IN-14: Define versioned forkable design capsules carrying operations, design fragments, requirements, data-only scenarios, review, BOM options, pack hashes, canonical evidence IDs, license, and learning material; instantiate through typed operations, keep custom logic in CSP-safe boundaries, and preserve unsupported/corrupt imports through IN-01. | Effort: XL | Priority: P2
- [ ] IN-15: Add photo-to-graph physical reconciliation with hashed images, scale/fiducial calibration, top/bottom alignment, human-confirmed suggestions, stable graph anchors, and separate intended/observed state. | Effort: XL | Priority: P2
- [ ] IN-16: Implement a version-pinned editable KiCad bridge plus open embed: import/export typed operations, preserve/report unsupported records, run semantic and native-file round-trip fixtures, and cross-probe stable anchors. | Effort: XL | Priority: P2
- [ ] IN-17: Build the living evidence garden on the existing pipeline: capture raw outside material in `inbox/`, run `/extract` into canonical `knowledge/`, publish/query versioned `pp-core`/`pp-hardware` sources through the manifest-tracked PP-NLM bridge, return Studio output to `inbox/`, and let product schemas store only resolvable evidence IDs/revisions/hashes/applicability/recheck state. | Effort: L | Priority: P2
- [ ] IN-18: Build signed reproducible open-hardware release capsules containing exact design/pack/deck/model revisions, canonical evidence IDs, licenses, policy-equivalent checks/scenarios/reviews, fabrication files, hashes, and truthful external status; require current audit plus independent packaged smoke for any desktop-generation claim. | Effort: L | Priority: P2
- [ ] IN-19: Run representative real legacy projects as a repeatable migration proving corpus; preserve originals, compare every projection/output, exercise browser/Tauri recovery and production CSP, freeze edge cases as fixtures, and gate the canonical-UI transition on explained losslessness. | Effort: XL | Priority: P2
- [ ] IN-20: Execute the ADR-0017 physical-system hypothesis ladder only after IN-01: real firmware co-design workflows, then bounded CadQuery/JSCAD enclosure and URDF link/joint experiments if evidence supports each next step; record ship/reshape/shelf at every gate, persist positive and negative results through IN-17's existing knowledge path, and use durable capability/learning language in any accepted ADR revision. | Effort: L | Priority: P3
