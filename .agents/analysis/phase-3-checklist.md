# Phase 3 Checklist — UX Issues (UI-)

> Priority is dependency/build order only: P0 foundations unblock P1, then P2, then P3. It is not a value ranking.  
> Effort: S (hours) / M (days) / L (weeks) / XL (months).

All findings get addressed — do not rank by priority/value/impact, do not fix only a subset, do not shrink the list.

- [ ] UI-01: Restore Vault, Labs, Audit Trail, Supply Chain, BOM Templates, My Parts, Alternates, and Part Usage in legacy now, add a visible/preview/retired invariant, and map every view to an engine capability projection or explicit retirement; derive engine navigation from that single registry (`client/src/components/layout/sidebar/sidebar-constants.ts:62-83`, `client/src/lib/role-presets.ts:126-154`, `client/src/components/layout/Sidebar.tsx:423-450`) | Effort: M | Priority: P0
- [ ] UI-02: Implement “Open sample” now in legacy so it transactionally creates the declared nodes, edges, and BOM, shows progress/failure, and lands on a populated step; freeze every sample as a fixture that the engine must recreate through typed operations before flip (`client/src/lib/sample-projects.ts:1-7`, `client/src/lib/sample-projects.ts:59-73`, `client/src/pages/ProjectPickerPage.tsx:613-621`) | Effort: L | Priority: P0
- [ ] UI-03: Remove the rich legacy template claim now and label the actual description-only behavior; recreate DRC, BOM, export, naming, and component policies as typed engine operations with a saved summary before flip (`client/src/pages/ProjectPickerPage.tsx:843-912`, `client/src/lib/team-templates.ts:67-76`, `client/src/pages/ProjectPickerPage.tsx:598-604`) | Effort: L | Priority: P0
- [ ] UI-04: Remove/disable every handlerless legacy radial action now; recreate the useful commands in the engine with eligibility, typed operations, undo, success/failure state, and command-discoverability evidence (`client/src/pages/ProjectWorkspace.tsx:372-400`, `client/src/pages/ProjectWorkspace.tsx:756-763`) | Effort: L | Priority: P0
- [ ] UI-05: Hide legacy milestone facts backed only by hard-coded flags now; recreate progress as a graph-derived engine projection over operations, checks, simulation, export, PCB, fab, variables, review, and community evidence, with partial-project re-entry tests (`client/src/pages/ProjectWorkspace.tsx:548-593`) | Effort: L | Priority: P0
- [ ] UI-06: Define and render one explicit product transition with a per-capability disposition ledger: identify the current canonical editor, provide loss-reporting import/handoff, prove real projects and required projections, then execute the default flip and read-only grace period without dropping any legacy promise silently (`client/src/App.tsx:81-89`, `packages/app/src/App.tsx:333-348`, `ROADMAP.md:2113-2142`) | Effort: XL | Priority: P0
- [ ] UI-07: Make the engine editor reflow safely: collapsible/resizable palette and inspector, persisted widths, usable narrow layouts, and no clipped/trapped controls (`packages/app/src/App.tsx:333-348`, `packages/app/src/styles.css:72-89`) | Effort: L | Priority: P1
- [ ] UI-08: Replace the 17–18 equal-width strip with a scalable capability/projection registry and hierarchy/overflow model; derive panels from it and implement WAI-ARIA tabs, arrow navigation, active announcement, and panel relationships (`packages/app/src/App.tsx:38-63`, `packages/app/src/App.tsx:291-329`, `packages/app/src/styles.css:222-247`) | Effort: L | Priority: P1
- [ ] UI-09: Build semantic circuit mode as a projection of the canonical graph plus ephemeral selection/tool/status state—not a parallel stored model: focusable named canvas, graph-derived DOM tree, announcements, keyboard commands, nonvisual properties, equivalent edits, and automated/manual assistive-technology evidence (`packages/app/src/state/session.ts:211-251`, `packages/app/src/state/ui.ts:84-145`, `packages/app/src/editor/CanvasHost.tsx:337-400`, `packages/app/src/editor/CanvasHost.tsx:524-524`) | Effort: XL | Priority: P1
- [ ] UI-10: Add explicit `:focus-visible` treatment for engine buttons, inputs, tabs, canvas, and tool controls, then verify it with a keyboard-only walkthrough and contrast checks (`packages/app/src/styles.css:35-68`) | Effort: M | Priority: P1
- [ ] UI-11: Hide/disable Draftsman now, then connect it to the proof-carrying crew runtime with real provider/tool/approval state; remove the window-global hook and expose the tab only when a user can complete and review the workflow (`ROADMAP.md:11-20`, `packages/app/src/panels/DraftsmanPanel.tsx:20-50`) | Effort: M | Priority: P1
- [ ] UI-12: Remove Settings from normal legacy navigation or label it Preview now; recreate canonical profile/appearance controls with validation/persistence and put provider secrets behind the private gateway before exposing API-key settings (`client/src/pages/settings/SettingsPage.tsx:7-18`, `client/src/pages/settings/SettingsPage.tsx:23-52`) | Effort: L | Priority: P1
- [ ] UI-13: Redirect malformed or non-positive project IDs to the project picker, preserve the attempted context where safe, and explain the correction instead of guessing project 1 (`client/src/pages/ProjectWorkspace.tsx:851-860`) | Effort: S | Priority: P1
- [ ] UI-14: Remove the hidden `/api/seed` wait from project entry so loading reflects the requested project only; move development fixtures to an explicit bootstrap flow with visible failure (`client/src/lib/project-context.tsx:217-244`, `server/routes/seed.ts:382-395`) | Effort: M | Priority: P1
- [ ] UI-15: Replace the legacy project-card `div role=button` pattern with semantic sibling controls: a real project link/button plus a separately focusable, always focus-revealable archive/restore action (`client/src/pages/ProjectPickerPage.tsx:229-270`, `client/src/components/ui/interactive-card.tsx:5-22`) | Effort: M | Priority: P2
- [ ] UI-16: Add machine-readable capability states (`available`, `local-only`, `emulated`, `preview`, `blocked`, `live`) and honest empty states now for flashing, community, board packs, quotes, and ordering; derive navigation/claims from integration truth and enable `live` only when each engine path returns verifiable evidence (`ROADMAP.md:254-254`, `ROADMAP.md:2091-2111`) | Effort: M | Priority: P2
- [ ] UI-17: Add an engine recovery screen for unreadable/future-version saved designs that preserves the original payload and offers export, retry/migrate, or explicit reset before any starter is flushed (`packages/app/src/state/persistence.ts:53-62`, `packages/app/src/main.tsx:24-25`, `packages/app/src/main.tsx:48-53`) | Effort: L | Priority: P2
