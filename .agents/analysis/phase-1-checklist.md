# Phase 1 Checklist — Enhancements (EN-)

> Priority is dependency/build order only: P0 foundations unblock P1, then P2, then P3. It is not a value ranking.  
> Effort: S (hours) / M (days) / L (weeks) / XL (months).

All findings get addressed — do not rank by priority/value/impact, do not fix only a subset, do not shrink the list.

- [ ] EN-01: Add `.ppx` version dispatch and forward migrations, an explicit unknown-op quarantine/preserve policy, a content-addressed asset store with op references, and frozen cross-version fixtures before expanding the graph vocabulary (`packages/graph/src/store/serialize.ts:20-26`, `packages/graph/src/store/serialize.ts:95-129`, `packages/graph/src/store/fs-store.ts:79-112`) | Effort: XL | Priority: P0
- [ ] EN-02: Make engine startup recovery non-destructive: retain the unreadable payload, stop unconditional starter overwrite, present export/reset/retry choices, and test corrupt plus future-version bundles (`packages/app/src/state/persistence.ts:53-62`, `packages/app/src/main.tsx:24-25`, `packages/app/src/main.tsx:48-53`) | Effort: L | Priority: P0
- [ ] EN-03: Replace the implicit per-project `/api/seed` gate with an explicit, idempotent development bootstrap that does not delay every project load or mutate the first unrelated project (`client/src/lib/project-context.tsx:217-244`, `server/routes/seed.ts:382-395`) | Effort: M | Priority: P0
- [ ] EN-04: Implement sample initialization now in the shipping legacy app: transactionally persist every declared node, edge, and BOM item, report partial failure safely, freeze all five samples as canonical parity fixtures, and require engine import/typed-op creation to materialize the same expected designs before the default flip (`client/src/lib/sample-projects.ts:1-14`, `client/src/lib/sample-projects.ts:59-73`, `client/src/pages/ProjectPickerPage.tsx:613-621`) | Effort: L | Priority: P1
- [ ] EN-05: Immediately reduce the legacy template UI to its real description-only behavior; define a canonical template contract, then recreate DRC, BOM, export, naming, and suggested-component application as typed engine operations/policies before the default flip—backport full legacy persistence only if templates must remain active through grace (`client/src/lib/team-templates.ts:67-76`, `client/src/lib/team-templates.ts:370-386`, `client/src/pages/ProjectPickerPage.tsx:598-604`) | Effort: L | Priority: P1
- [ ] EN-06: Repair legacy reachability now by restoring the eight omitted working views and adding an explicit visible/preview/retired invariant; create one engine capability/projection registry that derives navigation and maps every legacy view to engine parity or deliberate retirement without duplicating design truth (`client/src/lib/project-context.tsx:139-139`, `client/src/components/layout/sidebar/sidebar-constants.ts:48-84`, `client/src/lib/role-presets.ts:126-154`) | Effort: M | Priority: P1
- [ ] EN-07: Hide or label Draftsman unavailable immediately, then wire the panel to the shipped `@protopulse/ai` runtime through the proof-carrying provider/tool/approval boundary; remove the hidden global-hook dependency before exposing the tab (`ROADMAP.md:11-20`, `packages/app/src/panels/DraftsmanPanel.tsx:20-50`) | Effort: M | Priority: P1
- [ ] EN-08: Complete the migration milestone with a per-capability disposition ledger: run Tyler's real projects through the importer, record loss/parity reports, require every legacy promise to be implemented in engine or explicitly retired, flip only after acceptance, keep read-only grace, and retire areas through ADRs (`ROADMAP.md:2113-2142`) | Effort: XL | Priority: P1
- [ ] EN-09: Keep ADR-0017 outside current product claims until Tyler decides it; if accepted, assign backlog ownership and require the schema-evolution gate plus a cheap test of the same-user assumption before vertical work (`docs/adr/0017-physical-system-design-graph.md:1-6`, `docs/adr/0017-physical-system-design-graph.md:76-92`, `docs/adr/0017-physical-system-design-graph.md:117-150`) | Effort: S | Priority: P1
- [ ] EN-10: Label firmware authoring/emulation separately from live flashing now, then finish and verify WebSerial/WebUSB on named supported hardware with permission, disconnect, retry, and failure-state tests before enabling the engine capability state (`ROADMAP.md:235-265`) | Effort: L | Priority: P2
- [ ] EN-11: Close or explicitly bound the opportunistic ESP32 emulator long tail so support claims enumerate remaining GDMA, sleep/wake, interrupt, and peripheral limits (`ROADMAP.md:2045-2049`) | Effort: XL | Priority: P2
- [ ] EN-12: Label current part-pack travel `local-only` now, then decide and implement the engine publication model—hosting, identity, provenance, moderation, removal, and versioning—before enabling a live Community state (`ROADMAP.md:2083-2098`) | Effort: XL | Priority: P2
- [ ] EN-13: Build the verified board/module starter packs named in the roadmap after the ESP32 dependency is settled, preserving the hardware verification protocol for every physical claim (`ROADMAP.md:2093-2100`) | Effort: XL | Priority: P2
- [ ] EN-14: Label the current path export-only/blocked now; decide the quote/order account and API model, then add freshness, vendor failure, consent, and returned-evidence tests before enabling a live engine ordering state (`ROADMAP.md:2101-2111`) | Effort: XL | Priority: P2
- [ ] EN-15: Remove `/settings` from normal legacy navigation or label it Preview now; recreate only the canonical editor's real profile/appearance controls and route provider secrets through the private gateway boundary before exposing API-key settings (`client/src/pages/settings/SettingsPage.tsx:7-18`, `client/src/pages/settings/SettingsPage.tsx:23-52`) | Effort: L | Priority: P2
- [ ] EN-16: Resolve or formally exception the current Tauri `cargo audit` blockers and require a green desktop packaging lane before describing desktop delivery as healthy (`.agents/analysis/phase-0-metrics.md:127-140`) | Effort: M | Priority: P2
- [ ] EN-17: Complete the remaining manual `pcbnew` import verification and record the exact fixture, output, and acceptance result in the golden-contract evidence trail (`ROADMAP.md:36-47`) | Effort: S | Priority: P2
- [ ] EN-18: Replace hand-maintained repository counts and maturity prose with one generated current-state snapshot that labels generation time and separates shipped, in-progress, proposed, and historical truth (`.ref/project-dna.md:1-20`, `.agents/analysis/phase-0-metrics.md:1-12`) | Effort: M | Priority: P3
