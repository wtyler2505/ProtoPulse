import * as React from 'react';

/**
 * EmbedDialog — from rest-express@1.0.0.
 */
export interface EmbedDialogProps {
  [key: string]: unknown;
}

export declare const EmbedDialog: React.ComponentType<EmbedDialogProps>;
