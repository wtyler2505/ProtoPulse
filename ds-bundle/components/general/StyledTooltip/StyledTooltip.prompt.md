StyledTooltip from rest-express. Use via `window.ProtoPulse.StyledTooltip` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<TooltipProvider>` (full provider chain in README.md — components read theme/i18n from that context).

Wrapper around shadcn/ui Tooltip with consistent ProtoPulse styling.
Use this instead of manually composing Tooltip + TooltipTrigger + TooltipContent.
